# 05_mapping_leaflet_optional.R
# Optional mapping template (leaflet). Customize based on your Result objects.

source("scripts/00_setup.R")
suppressPackageStartupMessages({ library(leaflet) })

message("Template only: add code to extract positions from FLightR Result and map them.")
